# config.py —— 例程 02：WiFi 热点 + 文件浏览服务器 配置
#
# 改这里的值即可自定义热点名/密码/端口，改完重新上传本文件并复位板子。

# ---- WiFi 热点（AP）配置 ----
AP_SSID = "ESP-AP"            # 热点名称（手机搜索到的 WiFi 名）
AP_PASSWORD = "12345678"      # 热点密码（WPA2 要求至少 8 位；空字符串 "" = 开放网络）
AP_MAX_CLIENTS = 2            # 允许同时连接的设备数量
AP_CHANNEL = 1                # 信道 1~13（默认 1）

# ---- HTTP 服务器 ----
SERVER_PORT = 80              # 手机浏览器访问的端口（80 可直接省略不写）

# ---- 状态灯（WS2812）----
WS2812_PIN = 48               # 例程 01 探测确认：灯珠接 GPIO48
